# Cleanup Report

## Scope

This pass cleaned and documented generated evaluation artifacts only. It did not retrain models, rebuild FAISS, change model artifacts, or alter diagnosis behavior.

## Files Removed

No source files or evaluation artifacts were permanently removed.

Reason: all non-final evaluation files may still be useful for thesis discussion, historical comparison, or before/after RAG analysis, so they are preserved under `data/evaluation/archive/`.

## Files Moved To Archive

At the time of this cleanup pass, obsolete/intermediate files were already present under:

- `data/evaluation/archive/rag_diagnostics/`
- `data/evaluation/archive/rag_natural/`
- `data/evaluation/archive/discussion/`
- `data/evaluation/archive/real_chat/`

No additional active RAG diagnostic files needed to be moved because `data/evaluation/rag_diagnostics/` already contained only the final canonical artifacts.

## Dead Code Review

Reviewed script inventory under `backend/scripts/`:

- `evaluate_rag_retrieval_quality.py`: active; reproduces smoke/expanded RAG retrieval evaluations.
- `verify_ddxplus_completeness.py`: active; verifies 49-label DDXPlus scope and FAISS completeness.
- `rag_health_check.py`: active; CLI health check for RAG/model artifact loading and safety probes.
- `rebuild_faiss_from_ddx.py`: retained; required only if data changes and FAISS rebuild is intentionally requested.
- `train_clinicalbert_classifier.py`: retained; required only if label space or data changes.
- `evaluate_rag_confusion.py`: retained; historical/full retrieval evaluation utility for thesis discussion.
- `build_discussion_evaluation.py`: retained; generates discussion-ready evaluation pack from existing artifacts.
- `evaluate_pipeline_end_to_end.py`, `generate_real_chat_cases.py`, `analyze_pipeline_failures.py`: retained; support end-to-end discussion/evaluation workflows.
- OCR, normalization, environment, ingestion, and smoke-test utilities: retained because they are outside the RAG artifact cleanup scope and may support reproducibility.

No temporary debug scripts or commented-out investigation blocks were found that were safe to remove without risking thesis/discussion reproducibility.

## Stale References Updated

Updated references that would have written active outputs outside the standardized locations:

- `backend/scripts/build_discussion_evaluation.py`
  - Changed the generated full retrieval command output path from `data/evaluation/rag_natural` to `data/evaluation/archive/rag_natural`.
  - Reordered RAG metrics lookup to prefer `data/evaluation/archive/rag_natural/rag_metrics_summary.json`.
- `backend/scripts/verify_ddxplus_completeness.py`
  - Reordered RAG metrics lookup to prefer the archived `rag_natural` metrics path.

Remaining references to `data/evaluation/rag_natural` are fallback compatibility paths only, not active output destinations.

## Documentation Added

- `data/evaluation/cleanup_audit_report.md`
- `backend/docs/CLEANUP_REPORT.md`
- `backend/docs/PROJECT_ARTIFACTS_STRUCTURE.md`

## Autogenerated Cache Files

Python `__pycache__` directories were identified as autogenerated duplicates. A recursive delete command was rejected by the execution policy, so cache removal was not forced. These directories are not active project artifacts and can be regenerated by Python at any time.

Pytest validation with `--basetemp` created `.pytest_tmp_validation`. The sandbox denied reading/removing that directory afterward, so it is documented rather than force-deleted. It is a validation temp directory, not a required project artifact.

## Validation Notes

Compile checks passed for modified scripts.

The full selected pytest suite could not complete in the restricted sandbox because pytest could not access its temp directory during `tmp_path` fixture setup/cleanup. Re-running the same suite while excluding the four `tmp_path`-dependent `MedicalCaseSearcher` metadata-loading tests passed:

```text
35 passed, 4 deselected, 1 warning
```

Those four tests passed in the prior unrestricted validation run and were not changed by this cleanup pass.

## Reproducibility Status

Final RAG evaluation remains reproducible with:

```powershell
python backend\scripts\verify_ddxplus_completeness.py
python backend\scripts\evaluate_rag_retrieval_quality.py --case-set expanded --run-label expanded_final
python backend\scripts\rag_health_check.py --pretty
```
